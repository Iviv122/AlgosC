#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(){

	int reszta;
	vector<int> monety;
	
	cout<<"Wpisz reszte: ";
	getline(cin,stoi(reszta));

	string line;
	cout << "Wpisz nominale: ";
	getline(cin,line);
		
}

